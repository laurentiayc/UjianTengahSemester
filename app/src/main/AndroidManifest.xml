<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ujiantengahsemester">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewDaftarMhs"></activity>
        <activity android:name=".DataDiriAdminActivity" />
        <activity android:name=".RecyclerViewDaftarDosen" />
        <activity android:name=".RecyclerViewKelas" />
        <activity android:name=".HomeDosen" />
        <activity android:name=".Dosen.DataDiriMahasiswaActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".RecyclerViewDaftarMatkul" />
        <activity android:name=".RecyclerViewDaftarKrs" />
        <activity android:name=".Admin.RecyclerViewDataDosen" />
        <activity android:name=".CreateMahasiswaActivity" />
        <activity android:name=".CreateMatkulActivity" />
        <activity android:name=".CreateKrsActivity" />
        <activity android:name=".CreateDosenActivity" />
        <activity android:name=".HomeAdmin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>